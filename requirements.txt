pandas==2.0.1
numpy==1.26.4
matplotlib==3.7.1
seaborn==0.12.2
scikit-learn==1.2.2
xgboost==2.1.3
```

This lists the exact versions of the libraries used in your project. If you need different versions or specific versions based on your environment, you can adjust the version numbers accordingly. To install these dependencies, users can simply run:

```bash
pip install -r requirements.txt
```
